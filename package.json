{
  "name": "nabu",
  "version": "1.0.0",
  "private": true,
  "description": "MangaDex new-chapter notifier — a Cloudflare Worker that emails a digest of new chapters via Microsoft Graph.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260827.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.127.0"
  }
}
